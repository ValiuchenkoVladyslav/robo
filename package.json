{
  "name": "robo",
  "private": true,
  "workspaces": ["./frontend"],
  "scripts": {
    "types:gen": "cargo test",
    "types:check": "bun svelte-kit sync && bun svelte-check",
    "dev": "bun run scripts.ts dev",
    "build": "bun run scripts.ts build",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "^1.1.17",
    "husky": "^9.1.7",
    "typescript": "^5.7.3"
  }
}
